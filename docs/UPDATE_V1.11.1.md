# KickPulse v1.11.1

This patch allows Next.js Image to load team crests from Football-Data's official crest CDN:

- `https://crests.football-data.org/**`
- Existing API-Sports football image support remains enabled.

After replacing `next.config.mjs`, stop and restart the Next.js development server so the configuration is reloaded.
